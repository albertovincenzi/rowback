# Binaries
/rowback
/rowback-desktop
desktop/rowback
desktop/rowback-desktop
*.exe

# macOS app bundle (regenerated by desktop/build-app.sh)
desktop/*.app/
desktop/icon/AppIcon.iconset/

# Dumps, indexes, and generated restore SQL — never commit data
*.sql
*.idx.json
restore_*.sql
restore.sql

# Logs / scratch
*.log
preview.png

# OS / editor
.DS_Store
